# MagicSquare_260604 — 세션 3 Cursor 설계 보고서 (02)

> 작성 기준: 세션 3 워크북 · Harness + Rule 초안 · Git 원격 동기화  
> 선행 문서: [01.MagicSquare_ProblemDefinition_Report.md](01.MagicSquare_ProblemDefinition_Report.md)  
> Transcript: [Prompting/02.MagicSquare_Session3_CursorDesign-Transcript.md](../Prompting/02.MagicSquare_Session3_CursorDesign-Transcript.md)

---

## STEP 1 — 주제 (1문장)

**4×4 부분 마방진(빈칸 2개)에서 10선 판정 누락(특히 `/`)으로 시간을 낭비하는 문제를, ECB pytest Harness와 Cursor Rule 초안으로 “기계적·재현 가능한 TDD 작업장”을 마련한다.**

(Mom Test · Report/01 STEP 3과 동일)

---

## STEP 2 — 세션 3 산출물 목록

| 계층 | 산출물 | 경로 | 상태 |
|------|--------|------|------|
| **Harness** | pytest + ECB 골격 | `pyproject.toml`, `src/`, `tests/` | ✅ |
| **Git** | 원격 브랜치 7개 동기화 | `origin` (GitHub) | ✅ |
| **Rule** | `.cursorrules` | 루트 | ⏳ **미작성** (참고: `MagicSquare_1004/.cursorrules` 63줄 초안) |
| **Skill** | `magic-square-tdd` | `.cursor/skills/` | ❌ |
| **Command** | `/tdd-red`, `/review-ecb` | `.cursor/commands/` | ❌ |
| **Hook** | sessionStart, postToolUse | `.cursor/hooks.json` | ❌ |
| **Test Loop** | RED pytest FAIL | `tests/entity/test_d_*.py` | ❌ |

---

## STEP 3 — Harness 요약

### 디렉터리 구조

```
MagicSquare_260604/
├── pyproject.toml
├── src/
│   ├── entity/__init__.py
│   ├── control/__init__.py
│   └── boundary/__init__.py
└── tests/
    ├── entity/__init__.py
    ├── control/__init__.py
    └── boundary/__init__.py
```

### `pyproject.toml`

| 항목 | 값 |
|------|-----|
| 패키지명 | `magic-square-260604` |
| Python | `>=3.10` |
| dev 의존성 | `pytest>=8.0` |
| pytest | `testpaths = ["tests"]`, `pythonpath = ["src"]` |

### 검증

```powershell
pip install -e ".[dev]"
python -m pytest --collect-only
# → collected 0 items (테스트 본문 없음 — 정상)
```

### Harness 메모

- `tests/entity/__init__.py` 등은 골격용. RED 작성 시 `src/entity` import **shadow** 가능 — MagicSquare_1004 세션 5에서 삭제 사례 있음.
- 구현·테스트 **본문은 의도적으로 미작성** (Harness만).

---

## STEP 4 — Git · GitHub 동기화

| 항목 | 내용 |
|------|------|
| 저장소 | https://github.com/somin926kim/MagicSquare_260604 |
| 커밋 (spec) | `3494cf0` — Add ECB + Dual-Track TDD pytest harness skeleton. |
| 푸시 브랜치 | `main`, `spec`, `red`, `green`, `refactoring`, `staging`, `new_features` |
| Harness 포함 | **`spec` 브랜치만** (`3494cf0`). 나머지는 Initial commit (`292fc8e`) |

---

## STEP 5 — `.cursorrules` 초안 (참고 프로젝트)

본 저장소(260604)에는 아직 `.cursorrules` 없음. 동일 세션에서 **MagicSquare_1004** 루트에 63줄 초안 작성됨 — 260604 이식 시 SSOT로 활용.

| 영역 | 핵심 |
|------|------|
| 도메인 | 4×4, 빈칸 0×2, 1~16, 10선=34, 출력 `int[6]` 1-index |
| 오류 | boundary E001~E007, entity E001~E005 처리 금지 |
| ECB | boundary→control→entity, entity→\* import 금지 |
| Dual-Track | Logic Mock 금지 / UI Mock 허용, `D-*`/`U-*` |
| TDD | RED→GREEN→REFACTOR, skip/xfail/assert 완화 금지, MagicConstant SSOT |
| AI | 한국어, Phase/Layer/Track 선언, commit은 요청 시만 |

---

## STEP 6 — 8계층 + Hook 점검

| 계층 | 상태 | 근거 |
|------|------|------|
| **Model** | ⚠️ | Cursor 기본 LLM. 프로젝트 전용 Model 설정 없음 |
| **Agent** | ⚠️ | Harness·Rule 작성만. Ask/Agent 명문 규칙 없음 |
| **Harness** | ✅ | `pyproject.toml` + ECB `src/`·`tests/` 골격, pytest collect OK |
| **Rule** | ⏳ | 260604 미작성. 1004 초안 참고 |
| **Skill** | ❌ | 미작성 |
| **Command** | ❌ | 미작성 |
| **Tool/MCP** | ⚠️ | Shell(pytest) 전제. RED FAIL 로그 없음 |
| **Test/Review Loop** | ❌ | `test_d_*` 0개. Mom Test SC-1~2 **pytest 미증명** |
| **Hook** | ❌ | `hooks.json` 없음 |

---

## STEP 7 — Test/Review Loop 성공 기준 (세션 3)

| ID | 기준 | 검증 | 상태 |
|----|------|------|------|
| SC-L1 | Rule 존재 | `.cursorrules` (260604) | ⏳ |
| SC-L2 | Harness pytest | `python -m pytest --collect-only` | ✅ |
| SC-L3 | ECB 골격 | `src/{entity,control,boundary}` | ✅ |
| SC-L4 | pytest RED FAIL | `test_d_val_04` 등 FAIL | ❌ |
| SC-L5 | Git 원격 | 7브랜치 push | ✅ |

---

## STEP 8 — 세션 3 범위 대비

### In (완료)

- ECB + Dual-Track pytest Harness 골격
- GitHub 7브랜치 동기화 (TDD 워크플로 브랜치 구조)
- Report 02 · Transcript 02 Export

### Out (의도적 미포함 · 미완)

- 260604 `.cursorrules` · Skill · Command · Hook
- Domain GREEN 구현 · PyQt UI
- **RED 테스트 1개 + pytest FAIL 증거** ← Loop **미완**

---

## STEP 9 — 권고 (1건)

**Rule·Loop 빈 구멍:** Harness는 준비됐으나 `.cursorrules`와 `test_d_val_04`(반대 대각선 `/`) RED가 없어 Mom Test가 **pytest로 재현되지 않음**. 다음 Ask: (1) 260604 `.cursorrules` 이식 (2) `/tdd-red` → D-VAL-04 RED → FAIL 확인.

---

## STEP 10 — 다음 세션(4) 제안

1. `.cursorrules` — 1004 초안 → 260604 루트 이식·프로젝트명 정정
2. `/tdd-red` Command · `magic-square-tdd` Skill (선택)
3. D-VAL-04 RED (`/`) → pytest FAIL — Mom Test 기계 재현
4. (선택) `spec` Harness를 `red` 등 브랜치에 merge 전략 확정

---

## 부록 — 파일 트리 (현재)

```
MagicSquare_260604/
├── pyproject.toml
├── src/
│   ├── entity/__init__.py
│   ├── control/__init__.py
│   └── boundary/__init__.py
├── tests/
│   ├── entity/__init__.py
│   ├── control/__init__.py
│   └── boundary/__init__.py
├── docs/PRD.md
├── Report/
│   ├── 01.MagicSquare_ProblemDefinition_Report.md
│   └── 02.MagicSquare_Session3_CursorDesign_Report.md  ← 본 문서
└── Prompting/
    ├── 01.MagicSquare_ProblemDefinition-Transcript.md
    └── 02.MagicSquare_Session3_CursorDesign-Transcript.md
```
